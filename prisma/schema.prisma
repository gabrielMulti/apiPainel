generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Actions {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  idAction  Int
  table     String
  topicName String
}
